[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
python_files = test_*.py
filterwarnings =
    ignore:Core Pydantic V1 functionality isn't compatible:UserWarning
markers =
    integration: optional live Firecrawl tests (see tests/test_integration_firecrawl.py)
